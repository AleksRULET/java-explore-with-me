CREATE TABLE IF NOT EXISTS HITS
(
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY CONSTRAINT HIT_PK PRIMARY KEY ,
    APP VARCHAR(55) NOT NULL,
    URI VARCHAR(55) NOT NULL,
    IP VARCHAR(55) NOT NULL,
    CREATED TIMESTAMP NOT NULL
);